# 记忆点 2026-03-14

## 恢复指令
> 新会话开始时说："读取 20260314-记忆点.md 恢复上下文"

---

## 一、项目状态

- **项目名**: SkillGuard（原名 Skill-safe-plan，已重命名）
- **路径**: `D:\Xuxianbang-Skills\SkillGuard`
- **GitHub**: `https://github.com/xuxianbang1993/SkillGuard.git`（已推送 3 次 commit）
- **版本**: v5.2
- **安全评分**: ~9/10
- **Phase 0-3 + v5.1**: 全部完成

## 二、文件清单

```
SkillGuard\
├── README.md                    策略文档 v5.1
├── skillguard-gate.sh           PreToolUse Hook — Bash 拦截器（含凭证机制）
├── skillguard-audit.sh          审查主控脚本 v5.0（Layer 0-3 + SHA256 + CVE预检）
├── skillguard-write.sh          PreToolUse Hook — Write/Edit 写入守卫
├── 一键配置.sh                   一键安装（环境检测 + Hook配置 + 镜像构建 + 红队测试）
├── Dockerfile.skillguard        Layer 2 Docker 扫描镜像定义
├── run-tests.sh                 红队测试运行器（10个样本）
├── test-fixtures\               10个攻击模式测试样本（01-10）
├── .approved\                   审查通过凭证目录（一次性凭证，用后即删）
├── 20260313-checkpoint.md       旧会话检查点（Phase 0-3 修复详情）
└── 20260314-记忆点.md            本文件
```

## 三、关键决策记录

1. **品牌**: SkillGuard（用户选定，中英混合方案被否决）
2. **Docker 镜像**: `skillguard`（已删除旧镜像 `skill-sandbox`，需要时重建）
3. **全局生效**: Hook 写入 `~/.claude/settings.json`（全局配置），任意目录启动 Claude Code 均生效
4. **动态路径**: 一键配置.sh 用 `$(dirname "${BASH_SOURCE[0]}")` 获取安装路径，不硬编码
5. **当前未激活**: 用户的 settings.json 中尚无 SkillGuard Hook，等正式安装时执行一键配置.sh
6. **v5.2 凭证改为一次性**: 验证后立即删除，每次安装必须重新审查（用户要求）

## 四、settings.json 现状

用户当前 `~/.claude/settings.json` **未包含 SkillGuard Hook**，仅有：
- env: ANTHROPIC_AUTH_TOKEN / BASE_URL / AGENT_TEAMS
- hooks: TaskCompleted / TeammateIdle (exit 0) + UserPromptSubmit (claudeception)
- statusLine: micucodeline
- plugins: claude-mem

## 五、环境信息

| 组件 | 状态 |
|------|------|
| Windows 11 家庭版 | ✅（不支持 Windows Sandbox） |
| Docker Desktop 29.2.1 | ✅ |
| Docker Sandbox (microVM) | ✅ |
| Docker 镜像 skillguard | ❌ 已删除（需要时重建） |
| 火绒 | ✅ `C:\Program Files\Huorong\Sysdiag\bin\HipsMain.exe` |
| WSL2 + Ubuntu | ✅ |

## 六、GitHub 提交历史

1. `Initial release: SkillGuard v5.1` — 全部核心文件
2. `Add one-click hook installer` — 一键配置脚本
3. `Upgrade installer to full environment detection suite` — 8项环境检测

## 七、待办 / 下次可继续

- **正式安装**: 执行 `bash 一键配置.sh` 激活 Hook
- **旧目录清理**: `C:\Users\LENOVO\Desktop\临时文件\Skill-safe-plan\` 可手动删除
- **Phase 4+ 改进**: 见 README.md 第 10 节
  - P0: LLM-based 检测层 / mcp-scan 集成 / PostToolUse 监控 Hook
  - P1: CI 自动化 / Homoglyph 扩展 / 版本锁定 / 网络监控
  - P2: RAG 深度检测 / NLP 注入检测 / 供应链溯源 / Canary Token
